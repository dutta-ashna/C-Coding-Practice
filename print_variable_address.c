#include <stdio.h>

int main() {
    int num = 10;   // Declare an integer variable
    printf("Value of num: %d\n", num);          // Print the value
    printf("Address of num: %p\n", &num);       // Print the address using &
    return 0;
}